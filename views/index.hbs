<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.11.2/css/all.css">
    <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.11.2/css/v4-shims.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.5.3/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="/style.css">
    <title>Send Webhook</title>
    <script>
      function countChar(val) {
        var len = val.value.length;
        if (len >= 2000) {
          val.value = val.value.substring(0, 2000);
        } else {
          $('#charNum').text(`Content | ${2000 - len}`);
        }
      };
    </script>
</head>
<body>
    <nav>
        <h2>Discord Webhook</h2>
        <ul>
            <li><a href="https://raffsimms.com"><i class="fas fa-home"></i> Home</a></li>
        </ul>
    </nav>
<div class="container mt-4">
    <div class="jumbotron" style="margin-top: 50px;">
        <form action="/webhook/send" method="POST">
            <div class="form-group">
                <label for="webhook_url">Webhook URL</label>
                <input type="url" class="form-control" name="webhook_url" placeholder="https://discord.com/api/webhooks/">
            </div>
            <div class="form-group">
                <label for="username">Username</label>
                <input type="txt" class="form-control" name="username" placeholder="Username">
            </div>
            <div class="form-group">
                <label for="avatar_url">Avatar URL</label>
                <input type="url" class="form-control" name="avatar_url" placeholder="">
            </div>
            <div class="form-group">
                <label for="content"><div id="charNum">Content</div></label>
                <textarea class="form-control" onkeyup="countChar(this)" maxlength="2000" name="content" rows="5"></textarea>
            </div>
            <center><button type="submit" name="submit" style="width: 100%;" class="btn btn-dark">Send Webhook</button></center>
        </form>
    </div>
        {{#if message}}
        {{#unless status}}
            <div class="alert alert-danger" role="alert">
                {{ message }}
            </div>
        {{else}}
            <div class="alert alert-success" role="alert">
                {{ message }}
            </div>
        {{/unless}}
    {{/if}}
</div>
</body>
<script src="https://code.jquery.com/jquery-3.5.1.slim.min.js" ></script>
</html>